# lichess-electron
An electron client for lichess.org.

## Quickstart

First, clone:

`$ git clone https://github.com/seanpianka/lichess-electron.git`

Second, install:

`$ npm install`

Third, run:

`$ npm start`
